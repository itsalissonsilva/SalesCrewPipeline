# Optional (only needed for online insights)
OPENAI_API_KEY=sk-xxxx

# Path to your CSV inside the container. Leave as-is if you COPY the file at repo root.
SALES_CSV=/app/sales.csv

# Flask port (container)
PORT=5000
