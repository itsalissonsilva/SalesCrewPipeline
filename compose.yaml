services:
  web:
    build: .
    env_file: .env
    ports:
      - "5000:5000"       # host:container
    volumes:
      - ./data:/data:ro   # expects ./data/sales.csv on your host
    restart: unless-stopped